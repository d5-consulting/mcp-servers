services:
  pptx:
    build:
      context: ../..
      dockerfile: src/pptx/Dockerfile
    ports:
      - "8010:8010"
    volumes:
      - ./workspace:/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8010
