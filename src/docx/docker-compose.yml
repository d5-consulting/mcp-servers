services:
  docx:
    build:
      context: ../..
      dockerfile: src/docx/Dockerfile
    ports:
      - "8004:8004"
    volumes:
      - ./workspace:/workspace
      - ./src:/app/src/docx/src
    environment:
      - TRANSPORT=sse
      - PORT=8004
