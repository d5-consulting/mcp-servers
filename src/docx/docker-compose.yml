services:
  docx:
    build: .
    ports:
      - "8005:8005"
    volumes:
      - ./workspace:/workspace
      - ./src:/docx/src
    environment:
      - TRANSPORT=sse
      - PORT=8005
    command: make serve
