services:
  xlsx:
    build:
      context: ../..
      dockerfile: src/xlsx/Dockerfile
    ports:
      - "8013:8013"
    volumes:
      - ./workspace:/workspace
      - ./src:/app/src/xlsx/src
    environment:
      - TRANSPORT=sse
      - PORT=8013
