services:
  file-management:
    build:
      context: ../..
      dockerfile: src/file-management/Dockerfile
    ports:
      - "8005:8005"
    volumes:
      - ./workspace:/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8005
