services:
  o3-search:
    build:
      context: ../..
      dockerfile: src/o3-search/Dockerfile
    ports:
      - "8008:8008"
    volumes:
      - ./workspace:/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8008
      - OPENAI_API_KEY=${OPENAI_API_KEY}
